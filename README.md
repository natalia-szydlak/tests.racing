LeanLogics.com website
==============

For local development clone this repository and then start server inside of it:

``` bundle exec jekyll serve ```

Open you browser and point it to: ``` localhost:4000 ```
